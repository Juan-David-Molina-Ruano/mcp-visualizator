# Rust toolchain configuration for MCP Visualizer
# https://rust-lang.github.io/rustup/overrides.html#the-toolchain-file

[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
profile = "default"
